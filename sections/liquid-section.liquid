
<div data-section-id="{{ section.id }}">
  {{ section.settings.liquid }}
</div>

{% schema %}
{
    "name": "Liquid / HTML",
    "settings": [
        {
            "type": "liquid",
            "id": "liquid",
            "label": "Liquid Content"
        }
    ],
    "presets": [
    {
        "name": "Liquid / HTML"
    }
  ]
}

{% endschema %}